# Force LF line endings on text files so ruff format produces identical
# output regardless of the platform (Windows CRLF would otherwise drift).
* text=auto eol=lf

# Explicitly mark binary files so they are not normalized.
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
